name: Operação Hangar - Deploy Strategies

on:
  push:
    tags:
      - 'v*' # Dispara o pipeline estritamente quando criamos uma tag de versão (ex: v1.0.0)
      
# 🔐 ADICIONE ESTAS LINHAS AQUI PARA LIBERAR O ROBÔ:
permissions:
  contents: write
  actions: write

jobs:
  audit-and-simulate:
    name: Inspeção de Voo e Validação de Estratégias
    runs-on: ubuntu-latest
    steps:
      - name: Obter código do Hangar
        uses: actions/checkout@v4
        with:
          fetch-depth: 0 # Traz todo o histórico de tags para análise

      - name: Ler metadados da Tag Anotada
        run: |
          echo "=== EXAMINANDO CERTIFICADO DE VOO ==="
          git show ${{ github.ref_name }} --quiet

      - name: Validar Manifestos do Kubernetes
        run: |
          echo "Analisando consistência dos arquivos de infraestrutura da pasta /k8s..."
          if [ ! -f k8s/service-router.yml ]; then
            echo "ERRO CRÍTICO: Roteador de tráfego ausente!"
            exit 1
          fi
          echo "Todos os manifestos válidos e estruturados!"
          
      - name: Simulação de Deploy Seguro (Canary/Blue-Green)
        run: |
          TAG_ATUAL="${{ github.ref_name }}"
          echo "Iniciando deploy estratégico para a versão: $TAG_ATUAL"
          
          # Configura as credenciais globais do robô logo no início
          git config --global user.name "Hangar Copilot Bot"
          git config --global user.email "copilot@hangar.com"
          
          if [ "$TAG_ATUAL" == "v2.1.0" ]; then
             echo "🚨 ALERTA: Detectada anomalia crítica na telemetria da v2.1.0!"
             echo "🔥 Iniciando procedimento automático de ABORTAR DEPLOY (Rollback)."
             
             # CORREÇÃO: Garante o retorno para estável
             cat k8s/service-router.yml | sed 's/version: candidate/version: stable/g' > k8s/service-router.tmp
             mv k8s/service-router.tmp k8s/service-router.yml
             
             git add k8s/service-router.yml
             git commit -m "fix(rollback): força retorno para version: stable devido a falha na v2.1.0"
             git push origin main
             
             echo "✅ Sistema restaurado com sucesso para a versão de segurança anterior!"
             exit 1
          fi
          
          # CENÁRIO SUCESSO (v1.0.0): Ele vira a chave e SALVA no GitHub
          echo "Virando a chave do Roteador para a nova versão candidata..."
          cat k8s/service-router.yml | sed 's/version: stable/version: candidate/g' > k8s/service-router.tmp
          mv k8s/service-router.tmp k8s/service-router.yml
          
          git add k8s/service-router.yml
          git commit -m "build(deploy): rota atualizada para version: candidate via tag v1.0.0"
          git push origin main
          
          echo "Deploy concluído com sucesso e gravado no repositório!"